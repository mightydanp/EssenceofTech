package com.mightydanp.eot.common.handler;

public class EoTEventHandler {
	/*
	@SubscribeEvent
	public void LivingUpdateEvent(LivingEvent event) {
    if (event.getEntity() instanceof EntityPlayerMP) {
        EntityPlayerMP player = (EntityPlayerMP) event.getEntityLiving();
        InventoryPlayer inventory = player.inventory;
 
        if (inventory.hasItemStack(new ItemStack(Items.LAVA_BUCKET))) {
        	player.attackEntityFrom(DamageSource.lava, 0.5F);
        }
    }
 }
 */
}
